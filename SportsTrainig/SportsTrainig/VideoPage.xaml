<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="SportsTraining.Pages.VideoPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
    Title="{Binding Title}">

    <Grid RowDefinitions="Auto,*" Padding="0">
        <!-- Header with title and controls -->
        <Grid Grid.Row="0" BackgroundColor="{DynamicResource PageBackgroundColor}" Padding="12" RowSpacing="8">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <Label Grid.Row="0" Text="{Binding Title}" FontSize="20" FontAttributes="Bold" />

            <HorizontalStackLayout Grid.Row="1" Spacing="10">
                <Button Text="Play" Clicked="OnPlayClicked" />
                <Button Text="Pause" Clicked="OnPauseClicked" />
                <Button Text="Stop" Clicked="OnStopClicked" />
            </HorizontalStackLayout>
        </Grid>

        <!-- The actual MediaElement player -->
        <toolkit:MediaElement
            x:Name="Player"
            Grid.Row="1"
            Source="{Binding VideoUrl}"
            ShouldShowPlaybackControls="True"
            ShouldAutoPlay="True"
            Aspect="AspectFit"
            HorizontalOptions="FillAndExpand"
            VerticalOptions="FillAndExpand" />
    </Grid>
</ContentPage>