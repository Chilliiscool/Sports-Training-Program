<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="SportsTraining.Pages.VideoPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
    Title="{Binding Title}">
    <Grid RowDefinitions="Auto,*" Padding="0">
        <Grid BackgroundColor="{DynamicResource PageBackgroundColor}" Padding="12" RowSpacing="8">
            <Label Text="{Binding Title}" FontSize="20" FontAttributes="Bold" />
            <HorizontalStackLayout Spacing="10">
                <Button Text="Play"   Clicked="OnPlayClicked" />
                <Button Text="Pause"  Clicked="OnPauseClicked" />
                <Button Text="Stop"   Clicked="OnStopClicked" />
            </HorizontalStackLayout>
        </Grid>

        <!-- The actual player -->
        <toolkit:MediaElement
    x:Name="Player"
    Source="{Binding VideoUrl}"
    ShouldShowPlaybackControls="True"
    ShouldAutoPlay="True"
    Aspect="AspectFit" />
    </Grid>
</ContentPage>
